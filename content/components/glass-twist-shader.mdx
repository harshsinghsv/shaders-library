import GlassTwistShader from '@/components/website/GlassTwistShader';

export const metadata = {
  title: 'Glass Twist Shader',
  description: 'Transparent cyan glass ribbons with refraction and caustic effects',
};

## Preview

<div className="relative w-full h-96 rounded-lg overflow-hidden border border-gray-200 dark:border-gray-800 mb-8">
  <GlassTwistShader />
</div>

## Installation

Install the required dependencies:

```bash
npm install three @types/three
```

Or with yarn:

```bash
yarn add three @types/three
```

Or with pnpm:

```bash
pnpm add three @types/three
```

## Usage

<ComponentCodePreview name="glass-twist-shader" isTab={false} />

## Features

- **Glass Effect**: Transparent ribbons with refraction
- **Fresnel Lighting**: Edge reflections like real glass
- **Caustic Patterns**: Light patterns through glass
- **Light Background**: Soft gray-white gradient
- **Performance Optimized**: Efficient WebGL implementation
