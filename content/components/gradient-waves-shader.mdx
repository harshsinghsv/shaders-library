import GradientWavesShader from '@/components/website/GradientWavesShader';

export const metadata = {
  title: 'Gradient Waves Shader',
  description: 'Sleek minimalist waves with smooth gradients in blue and purple',
};

## Preview

<div className="relative w-full h-96 rounded-lg overflow-hidden border border-gray-200 dark:border-gray-800 mb-8">
  <GradientWavesShader />
</div>

## Installation

Install the required dependencies:

```bash
npm install three @types/three
```

Or with yarn:

```bash
yarn add three @types/three
```

Or with pnpm:

```bash
pnpm add three @types/three
```

## Usage

<ComponentCodePreview name="gradient-waves-shader" isTab={false} />

## Features

- **Minimalist Design**: Clean wave ribbons on black background
- **Smooth Gradients**: Blue to purple color transitions
- **Multiple Layers**: 5 layered waves with depth effect
- **Glow Effects**: Soft glow around each wave
- **Performance Optimized**: Efficient WebGL implementation
